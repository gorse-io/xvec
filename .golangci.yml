version: "2"

linters:
  exclusions:
    rules:
      # Snowball stemmers are generated from upstream language definitions.
      - path: internal/db/index/column/fts_column/tokenizer/snowball/
        linters:
          - staticcheck
        text: SA4004
      # Nil-context tests intentionally verify public API validation.
      - path: _test\.go
        linters:
          - staticcheck
        text: SA1012
